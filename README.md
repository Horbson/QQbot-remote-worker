# QQbot-remote-worker：一个远程执行命令用的QQ机器人

## 碎碎念

某天决定做点什么来练习一下。

然后看向了OpenClaw：这个东西对于我来说目前最大的用处是，可以通过QQ远程操控电脑、发给我我电脑上的文件。

但是这个东西需要Token，并且因为要等AI思考，每次很简单的任务也需要等一段时间。

但是但是，既然我只是想远程操作电脑，那我把命令传到shell里不就行了吗？

于是就做了这么个机器人。

## 使用准备
 - Python环境。需要的依赖库有：
    - openai==2.29.0
    - qq-botpy==1.2.1
    - dotenv
 - 在[腾讯开放平台](https://q.qq.com)注册一个QQ机器人，并把AppSecret和AppID填写到.env文件里。
 - 准备一个OpenAI兼容的大语言模型API。然后把base_url和api_key填到.env里。

## 运行
 - 准备好后，直接`python app.py`就能运行。

## 首次使用
 - 首次使用需要绑定管理员。跟随控制台上的提示，把验证码发送给机器人即可。
 > <s>其实大概率这个机器人只是用于一对一这纯粹是走个形式</s>

## 使用指南

机器人会把收到的消息分为两类：
1. `/`开头的消息：会作为命令处理。
2. 不是`/`开头的消息：会直接将消息内容交由当前激活模式的`message_handler`方法处理。

机器人同一时间只会激活一个模式。目前仅有两个模式：
1. **exec**（默认）：将消息作为命令行命令处理，返回执行结果。
2. **chat**：和AI助手进行对话。目前还仅仅只是对话而已。

目前可用命令：

基础命令：
 - `/help <mode>`: 获取帮助。未指定`mode`则获取基础命令帮助。
 - `/modes`: 列出所有可用模式。
 - `/switch <mode>`: 切换模式。未指定`mode`则默认切换到下一个模式。
 - `/active_mode`：查看当前激活的模式。

模式相关命令：
 - `/exec:set_cwd`：设定`exec`执行命令的工作目录。
 - `/exec:whereami`：查看`exec`当前工作目录
 - `/chat:new_session`：清空上下文。

## 扩展

模式使用了类似插件的方法管理。

自行编写新模式必须提供一个类作为接口，该类必须有如下方法：

 - `message_handler(self, message)`: 接受消息内容作为参数，返回要发送的消息内容。
 - `command_handler(self, command, arg=None)`: 接受命令后缀（例：`/exec:set_cwd`的`set_cwd`）和参数作为参数，返回要发送的消息内容。
 - `helper(self, arg=None)`：返回该模式的帮助信息。

编写完成后，需要将该模式添加到`mode/__init__.py`的`modes`列表中。

## 当前的问题与隐患
 - 仅在Windows平台上测试过。不知道在wsl/Linux环境下会出什么问题。
 - 调用大模型API的函数是照着DeepSeek写的，使用和返回了有可能是DeepSeek独有的字段`reasoning_content`。
    > 如果出现了这种情况，修改`mode/chat/llm_tools/llm_call`中的`call_llm`函数即可。
 - `exec`函数没有任何拦截与再确认机制。执行有风险的命令前请三思。并且运行时注意不要让别人能使用你的QQ号给机器人发消息。
 - `exec`函数在执行阻塞式的命令（如`python`, `start cmd`）时，超时可能不会正常生效，导致机器人被卡住。
 - 代码中可能突然出现的报错。

## 计划更新的内容
 - 完善日志记录。
 - 完善chat模式并且给AI助手搭建Agent框架，提供操作电脑的能力。<s>（所以为什么不直接给OpenClaw发消息呢？）</s>
 - 提供更多模式和命令，例如我最需要的将电脑上的某文件发送给我。
 - 改bug。

## 碎碎念*2

在用两节水课完成了第一个可用的版本后，我突然觉得我应该练一下自己的规范的软件工程能力。

> 但貌似Python实际上并不适合多人协作项目。

于是决定把模式改成插件式的，对原来的代码进行了一次重构。

<s>然后，一个小粪堆就此变成了一个初见端倪的史山。</s>

虽然做了模块化，但其实各个模块之间还是藕断丝连。并且`mode_manager`的设计很明显对扩展的考量不足。

>比如万一我需要加一个需要知道用户身份或者机器人名字的模式呢？

上传到GitHub的初衷是方便我在上课时用平板的Termux来顺着电脑上的进度写代码，但是后来觉得这东西或许有点用，遂公开。

不过不管有没有用也算是做成了一个小东西，还是有点成就感的。

（end）